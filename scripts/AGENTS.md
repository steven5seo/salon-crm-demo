# JavaScript Guidelines

- Write modern, framework-free ES modules; do not add bundlers.
- Keep functions pure where possible and document side effects with comments.
- Use `const` by default, `let` only when reassignment is required.
- Avoid global variables; export shared utilities from `utils.js`.
