# Stylesheet Guidelines

- Follow a mobile-first approach and group related rules together.
- Use BEM-style class names for new selectors.
- Prefer CSS custom properties defined in `theme.css` before adding new ones.
- Avoid inline styles; keep all styling here.
